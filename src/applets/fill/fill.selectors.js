export const selectFillHeight = state => state.height;

export const selectFillWidth = state => state.width;

export const selectFillColor = state => state.color;

export const selectFillSettingsIsFailed = state => state.isFailed;

export const selectFillSettingsIsLoading = state => state.isLoading;
